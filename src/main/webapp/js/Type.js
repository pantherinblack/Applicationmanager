/**
 * shows a type
 * @param data of the type
 */
function showType(data) {
    document.getElementById("typeName").value = data.typeName;
    document.getElementById("typeDesc").value = data.typeDescription;
}